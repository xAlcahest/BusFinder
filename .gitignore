node_modules/
.next/
out/
data/*.db
# -wal, -shm, -journal: SQLite ne crea di nuovi a piacere, quindi la regola
# li prende tutti invece di elencarli.
data/*.db-*
data/*.zip
data/tmp/
.env*.local
# Contengono dominio, host SSH e recapiti: solo .env.example va versionato.
.env
.env.server
next-env.d.ts
*.tsbuildinfo
